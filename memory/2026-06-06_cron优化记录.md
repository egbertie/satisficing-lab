# 2026-06-06 Cron 精简优化记录

> 23:32 用户提出双经济约束审查 · 过度设置违背原则

## 优化前 (6条, 日执行340次)
1. 备份 03:00 (1次)
2. 健康检查 */30min (48次) ❌ 冗余
3. 日志清理 周日 (0.14次) ❌ 与logrotate重复
4. 磁盘监控 08:00 (1次) ⚠️ 70GB空余，早期不需要
5. 日志轮转 --force (0.14次) ❌ 与系统logrotate冲突
6. 采集 */5min (288次) ❌ 监控面板刷新30s，采集1次/日够用

## 优化后 (3条, 日执行3次) · 减少99%
1. **备份** 03:00 (1次/日) — tar打包+配置文件
2. **健康简报** 08:00 (1次/日) — 合并:服务/磁盘/内存/备份/防火墙/采集
3. **日志轮转** 周日04:00 (1次/周) — logrotate+旧日志清理

## 删除的冗余
- 健康检查(48次/日) → systemd `Restart=always` 自带故障恢复
- 采集(288次/日) → 合并到早8点简报
- 磁盘监控(1次) → 合并到简报
- 日志清理 → 合并到日志轮转
- --force logrotate → 与系统自带冲突

## 关于本地模型
用户提出是否可用本地模型处理 Cron——可行：
- OpenClaw Cron 用 Flash 模型跑轻量任务
- 云端Cron 属于系统运维，bash 脚本即可，不需要模型
- 日起课/日毕课/综合守护 仍走 OpenClaw Cron (Mac) + 模型
