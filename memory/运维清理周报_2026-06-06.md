# 🧹 运维清理周报 — 2026-06-06 (周六)

## 一、脚本审计

| 清理项 | 操作 | 节省空间 | 备注 |
|--------|------|---------|------|
| `memory/_scripts/` 下所有 .py/.sh 脚本清单 | 已列出 60 个脚本 | — | 见下方详细列表 |
| 孤儿脚本排查 | 未发现孤儿脚本 | — | 唯一 cron 为本巡检任务，其他脚本为内部模块引用 |

### memory/_scripts/ 完整清单 (60 个)
- **.py (51个):** activate_assets_v2, add_quality_confidence, auto_evolution_engine, auto_update_entities_index, avatar_registry, build_kg_v3, check_blood_lines, coze_archive, coze_session_import, data_init_pipeline, external_learning_engine, feishu_token_guard, immune_engine, knowledge_orchestrator, llm_spectrum_refine, neural_trainer, patch_entities_v2, q_scan, q_tracker, quality_audit_12gate, seed_dashboard_data, spectrum_scorer, sri_antifragile_stressor, sri_auto_healer, sri_autocalibration, sri_bottleneck_breaker, sri_chemical_governor, sri_chemistry_reactor, sri_consistency_checker, sri_knowledge_flywheel, sri_lifecycle_manager, sri_nourishment_collector, sri_orchestrator, sri_perpetual_control, sri_portfolio_rationalizer, sri_product_scanner, sri_quality_gate, sri_second_order_observer, sri_standards_iterator, sri_vsm_system5, term_fix_site_only, term_sync_engine, token_health_monitor, write_spectrum_to_index
- **.sh (9个):** auto_heal, immune_scan, star_backup

## 二、数据文件去重

| 清理项 | 操作 | 节省空间 | 备注 |
|--------|------|---------|------|
| `memory/_scripts/` 下 .json 文件 | 无 — 0 个 json 文件 | — | ✅ 符合规范 |
| `memory/` 根目录 .json 文件 | 发现 2 个 | — | 详见下方 |
| `heartbeat-state.json` (87B) | **保留原位** | — | 系统运行时文件，仅此一份 |
| `entities_index.json` (341B) | ✅ **已移入 Archives** | 341B | 根目录旧备份，_data 下有 9.8MB 完整版 |
| `_data/` vs `_scripts/` 同名 .json 重复 | 无 | — | ✅ |

## 三、压缩泄漏清理

| 清理项 | 操作 | 节省空间 | 备注 |
|--------|------|---------|------|
| 文件名含"压缩前保存"的文件 | 未发现 | — | ✅ 无泄漏 |

## 四、临时文件检查

| 清理项 | 操作 | 节省空间 | 备注 |
|--------|------|---------|------|
| workspace 根目录 .html 文件 | **报告不删除** | ~4.3MB | 大量网页文件（约 200+ html 文件），均为 `sri` 项目产出的报告页面 |
| workspace 根目录 .png 文件 | **报告不删除** | ~1.3MB | `sri_compass_logo.png` (1.0MB)、`sri_dingyu_logo.png` (312KB) |
| workspace 根目录 .jpg 文件 | **报告不删除** | 736KB | `sri_main_logo.jpg` |
| **临时文件总计** | **本次不操作** | **~6.4MB** | 疑似为项目文件，需人工确认是否可删除 |

### 根目录临时文件清单 (部分)
- `sri_compass_logo.png` — 1.0MB
- `sri_main_logo.jpg` — 736KB
- `sri_dingyu_logo.png` — 312KB
- 约 **200+ 个 .html 文件** (2026-06-03 批量生成) — ~4.3MB

## 五、磁盘统计

| 目录 | 大小 | 备注 |
|------|------|------|
| `memory/_data/` | 42MB | 主要索引、图谱、状态数据 |
| `memory/_scripts/` | 864KB | 60 个脚本 |
| `memory/Archives/` | 40KB | 门控归档报告 |
| `memory/.dreams/` | 不存在 | — 未创建 → 无需检查 |
| 对话目录空文件夹 | 无 | `memory/对话` 目录不存在 → ✅ |

## 六、本次操作汇总

| 操作 | 文件数 | 节省空间 |
|------|--------|---------|
| 孤儿脚本 → 归档 | 0 | — |
| 根目录冗余 JSON → 归档 | 1 (entities_index.json) | 341B |
| 压缩泄漏文件 → 归档 | 0 | — |
| 临时文件报告（待确认） | ~200+ 文件 | ~6.4MB 待确认 |

**本次共释放空间: ~341 字节**

## 建议

1. **根目录 .html 文件**：约 200+ 个 html 文件占 ~4.3MB，疑似是一次性生成的网页报告。建议确认用途后可整体删除或移入子目录。
2. **根目录 logo 图片** (3个 png/jpg, ~2MB)：如已归档到项目中，可删除。
3. **目录结构**：`memory/对话/` 目录不存在，coze 归档脚本 (`coze_archive.py`) 尚未触发过归档——这可能是符合预期的。

[FIN-A]
