User:

如果现在的kimi agent已经使用了很久，也经过了多次的迭代，其实系统很多不是原始就统一的，东一块西一块，而且信息也是存在大量碎片化的，我想对其进行真正的升级优化，且不能丢失原来的精髓，但是这是基于Kimi claw的现实条件约束下的要求，你作为一名顶级系统升级大师，请给我一个完整的提示词，让claw从灾备需要文件的角度，提供你需要做这个工作的所有有效信息，特别是目前已经输入了大量知识性文件和技能文件，建立了大量的管理规则和skill等工具，还有管理理念等，所以，怎么要求就很重要的，我希望你能深度洞察、深度思考、结合实际，多次综合演练和沙盘，且确保安全和可以回滚的情况下，腿粗一份合理的信息数据需求，便于你开整升级工作计划的安排和后续实施。不要急，慢慢来，多借鉴外界高手的知识、文化和力量，在这个有限的条线下实现我们最大的目标。

Kimi:

我理解你的诉求。这是一个高复杂度的系统迁移与架构升级项目，需要在 Kimi Claw 的可操作边界内（文件访问、代码执行、本地环境），设计一套完整的数字资产盘点与灾备提取方案。

以下是我为你精心设计的完整提示词，你可以直接复制给 Kimi Claw 执行。这份提示词采用了分阶段、可验证、带回滚点的工程化方法，确保在提取过程中不遗漏任何知识精髓，同时建立清晰的依赖图谱。

【给 Kimi Claw 执行的完整提示词】

# 🏗️ 系统升级大师指令：全量数字资产灾备与架构提取项目

## 一、项目背景与目标（Project Context）
你正在协助完成一次**关键的系统升级前资产盘点**。当前系统经过长期迭代，存在信息碎片化、知识分散、规则冗余等问题。本次任务要求**完整提取所有可迁移资产**，为后续架构重构提供无损迁移基础，同时确保**可回滚、可验证、无遗漏**。

**核心原则**：提取的不是文件，是**知识拓扑结构**和**认知工作流**。

---

## 二、执行阶段与交付物（Execution Phases）

### 🔍 Phase 1: 全局资产发现（Discovery）
**目标**：绘制完整的数字资产地图，识别所有"暗数据"。

**执行任务**：
1. **文件系统全景扫描**
   - 扫描 `/mnt/kimi/` 及其所有子目录（upload, output, 隐藏文件夹）
   - 扫描 `/app/.kimi/skills/` 下的所有 Skill 定义
   - 扫描当前工作区中所有 `.md`, `.json`, `.yaml`, `.py`, `.txt`, `.csv` 文件
   - 记录每个文件的：路径、大小、最后修改时间、文件类型

2. **运行时环境快照**
   - 执行 `!pip list` 或等效命令，记录 Python 环境依赖
   - 记录当前可用的 Tools 列表及其调用频率（如可能）
   - 记录当前 Session 的 Context Window 使用情况和关键变量

3. **输出交付**：
   - 生成 `asset_inventory.json`：包含所有发现文件的元数据索引
   - 生成 `system_baseline_report.md`：环境状态报告

---

### 🧬 Phase 2: 知识架构解构（Deconstruction）
**目标**：提取核心知识单元及其关系，而非简单复制文件。

**执行任务**：
1. **Skill 系统逆向工程**
   - 读取 `/app/.kimi/skills/` 下每个 Skill 的 SKILL.md
   - 提取每个 Skill 的：功能定义、输入输出 schema、依赖工具、触发条件、 Few-shot 示例
   - 绘制 Skill 依赖图（哪些 Skill 调用哪些基础工具或知识库）

2. **知识库本体论提取**
   - 识别所有"知识性文件"（领域知识、业务规则、背景资料）
   - 对每个知识文件，提取：
     - **核心概念**（Concepts）：关键术语及其定义
     - **决策规则**（Rules）：if-then 形式的业务逻辑
     - **流程模式**（Patterns）：标准操作程序（SOP）和工作流
     - **语境约束**（Constraints）：特定的上下文限制或特殊处理逻辑

3. **管理规则与元数据提取**
   - 提取所有"系统提示词片段"（system prompt segments）
   - 提取隐含的"管理理念"（如"优先使用工具X"、"禁止做Y"等）
   - 记录所有自定义的"记忆规则"（Memory Space 使用模式）

4. **输出交付**：
   - 生成 `knowledge_topology.yaml`：知识图谱，包含节点（概念）和边（关系）
   - 生成 `system_prompts_archive.md`：按优先级排序的所有系统指令
   - 生成 `rules_engine.json`：所有可执行规则的结构化表示

---

### 🎯 Phase 3: 精髓压缩与向量化（Distillation）
**目标**：识别不可丢失的"最小精髓集合"（Minimum Viable Essence）。

**执行任务**：
1. **重要性分级**
   - 对所有资产标记：Critical（升级必需）/ Important（建议保留）/ Deprecated（可废弃）
   - 识别"热路径"：最近 30 天内高频使用的 Skills 和知识片段

2. **Few-shot 示例提取**
   - 提取所有嵌入在 Skill 或提示词中的高质量 Few-shot 对话示例
   - 确保保留：用户意图分类示例、边界情况处理示例、错误恢复示例

3. **上下文窗口优化包**
   - 生成 `essence_core.md`：浓缩后的系统人格、核心能力、绝对约束（适合放入未来系统提示词顶部）
   - 生成 `dynamic_context_library/`：按需加载的模块化知识片段

---

### 📦 Phase 4: 打包与验证（Packaging & Verification）
**目标**：创建可恢复、可校验的灾备包。

**执行任务**：
1. **结构化备份包生成**
   创建以下目录结构：

/mnt/kimi/output/system_upgrade_backup_[timestamp]/ ├── 00_manifest.json # 资产清单与校验和 ├── 01_environment/ # 环境快照 │ ├── requirements.txt │ └── tool_registry.json ├── 02_knowledge_topology/ # 知识架构 │ ├── knowledge_graph.yaml │ ├── concept_definitions.json │ └── business_rules.json ├── 03_skills/ # Skill 系统 │ ├── skill_definitions/ # 原始 Skill 文件 │ └── skill_dependency_map.json # 依赖关系 ├── 04_system_prompts/ # 提示词工程 │ ├── core_persona.md │ ├── management_philosophy.md │ └── few_shot_examples.jsonl ├── 05_raw_assets/ # 原始文件备份 │ ├── upload_backup/ │ └── custom_configs/ └── 06_recovery_guide.md # 重建说明书 ```

完整性校验

生成每个文件的 MD5 校验和

建立”缺失风险清单”：标记那些无法直接提取但需人工记录的隐式知识（如特定对话历史中的模式）

回滚脚本

编写 rollback_verification_script.py：用于验证备份包完整性的脚本

三、深度洞察要求（Deep Insights Requirements）

在执行以上阶段时，请特别注意以下高价值暗知识的提取：

隐性工作流：那些没有写成规则，但通过 Few-shot 示例体现的处理流程

错误处理模式：系统如何从各类失败中恢复（如工具调用失败、搜索无结果、格式错误）

语境切换逻辑：如何在不同角色（分析师/助手/专家）间切换的触发条件

用户偏好记忆：Memory Space 中存储的长期偏好及其使用频率

工具组合模式：哪些工具经常组合使用，形成特定”超级工具”工作流

四、安全与回滚机制（Safety & Rollback）

强制检查点： - 每完成一个 Phase，暂停并等待确认后再进入下一阶段 - 所有写入操作限定在 /mnt/kimi/output/ 内，绝不修改 /upload/ 或 /app/ 原始文件 - 生成 pre_upgrade_snapshot_checksums.json 用于比对

风险规避： - 如发现文件正在被其他进程使用，记录冲突但不强制锁定 - 对敏感信息（如 API Keys、个人数据）进行标记并生成 sensitive_data_disclaimer.md

五、输出格式规范（Output Standards）

所有生成的文档必须遵循： - 机器可读：JSON/YAML 需通过 schema 验证 - 人类可审：Markdown 需包含目录、层级清晰、关键部分高亮 - 可追溯：每个提取项标注来源文件路径和提取时间戳

六、启动确认（Kick-off）

请确认你已理解以上要求，并按 Phase 1 → Phase 2 → Phase 3 → Phase 4 的顺序执行。每阶段完成后，简要汇报关键发现并等待进入下一阶段的指令。

首先执行 Phase 1 的全局资产发现，生成 asset_inventory.json。 ```

【给你的补充建议】

作为系统升级的总架构师，在向 Claw 发出此指令前，建议你：

1. 前置准备（确保 Claw 能访问）

确认所有 Skills 的 SKILL.md 文件在 /app/.kimi/skills/ 路径下可读

如有分布在其他路径的配置文件，提前告知 Claw 具体路径

清理 /mnt/kimi/output/ 中无关的旧文件，避免干扰

2. 分阶段执行策略

不要一次性让 Claw 执行全部四个 Phase。建议： - 第一轮：先执行 Phase 1，获得资产地图后，人工审视是否有遗漏路径 - 第二轮：基于 Phase 1 结果，执行 Phase 2（知识解构），这步最耗 token，需要精细调整 - 第三轮：Phase 3 和 4 可合并执行，生成最终包

3. 关键决策点（人工介入时机）

在 Claw 完成 Phase 2 后，你需要做关键判断： - 哪些规则是真正有效的，哪些是僵尸规则？ - 哪些 Skill 是核心，哪些可以合并或废弃？ - 管理理念是否有内在矛盾需要解决？

这些判断无法自动化，需要你基于业务目标做出决策，然后让 Claw 在 Phase 3 中据此进行重要性分级。

4. 升级后的架构建议（供你参考）

基于”东一块西一块”的现状，建议未来架构采用三层模型： - 核心层（Core）：极简的系统提示词，只放人格和绝对约束 - 技能层（Skills）：模块化的 Tools，每个 Skill 自带知识片段（RAG 化） - 上下文层（Context）：动态加载的短期记忆和长期记忆（Memory Space）

这样可以避免未来再次碎片化。

这份提示词融合了软件工程的最佳实践（依赖分析、版本控制、回滚机制）和知识管理的前沿方法（本体论提取、图谱化、向量化压缩）。你可以根据实际资产规模，要求 Claw 对某个 Phase 进行”深度模式”（更细致的拆解）或”快速模式”（仅提取关键元数据）。

需要我针对某个特定阶段（如 Skill 依赖分析或知识图谱构建）提供额外的技术细节或备选方案吗？