---
# 知识元数据 (5标准化)
knowledge_id: W6-5FA33B
title: 飞书多维表格创建与协作指南（方案B）
category: 12_记忆档案
source: docs/archive/多维表格创建指南.md
ingested_at: 2026-03-27 17:59:30
word_count: 2048
week: 6
s1_input: true
s2_process: true
s3_output: true
s4_auto: true
s5_verify: true
s6_limit: true
s7_test: true
---

# 飞书多维表格创建与协作指南（方案B）

> **知识ID**: W6-5FA33B  
> **分类**: 12_记忆档案  
> **来源**: `docs/archive/多维表格创建指南.md`  
> **入库时间**: 2026-03-27

---

## 正文

# 飞书多维表格创建与协作指南（方案B）

**目标**：创建应用空间的多维表格，实现双向协作
**验证时间**：2026-03-09

---

## 第一步：创建多维表格（应用空间）

### 1.1 进入API调试台
- 登录：https://open.feishu.cn/app/cli_a9208a79e2b99cc4
- 找到「API调试台」

### 1.2 选择接口
- **分类**：`base`（多维表格）
- **接口**：`POST /open-apis/base/v1/apps`
- **名称**：「创建多维表格」/「Create App」

### 1.3 填写请求体
```json
{
  "name": "满意解研究所_案例库V2",
  "description": "应用空间创建的案例管理表格"
}
```

### 1.4 发送请求
- 点击「发送」
- **预期成功响应**：
```json
{
  "code": 0,
  "data": {
    "app": {
      "app_token": "xxxxxxxxxx",  // 重要！保存这个
      "name": "满意解研究所_案例库V2",
      "url": "https://feishu.cn/base/xxxxxxxx"
    }
  }
}
```

**保存**：`app_token`（后续步骤需要）

---

## 第二步：创建数据表（Table）

### 2.1 选择接口
- **接口**：`POST /open-apis/base/v1/apps/{app_token}/tables`

### 2.2 填写路径参数
- `app_token`：上一步获取的token

### 2.3 填写请求体
```json
{
  "table": {
    "name": "合伙人决策案例"
  }
}
```

### 2.4 发送请求
- **预期响应**：返回 `table_id`
- **保存**：`table_id`

---

## 第三步：添加字段（可选）

### 3.1 选择接口
- **接口**：`POST /open-apis/base/v1/apps/{app_token}/tables/{table_id}/fields`

### 3.2 示例字段
```json
{
  "field_name": "客户名称",
  "type": 1  // 1=文本
}
```

字段类型参考：
- `1` = 文本
- `2` = 数字
- `3` = 单选
- `4` = 多选
- `5` = 日期
- `18` = 人员

---

## 第四步：添加你为协作者

### 4.1 选择接口
- **分类**：`base`（多维表格）
- **接口**：`POST /open-apis/base/v1/apps/{app_token}/roles/{role_id}/members`
- 或搜索：「添加协作者」/「Create Collaborator」

### 4.2 填写参数
- `app_token`：第一步获取的
- `role_id`：`admin` 或 `editor`

### 4.3 填写请求体
```json
{
  "member_type": "openid",
  "member_id": "ou_aea2e859a6cee1f3a11e8c018eb88ec9",
  "perm": "edit"
}
```

### 4.4 发送请求
- **预期成功**：你成为表格协作者

---

## 第五步：验证协作

1. **打开表格链接**（第一步返回的url）
2. **检查**：
   - ✅ 能否看到表格？
   - ✅ 能否编辑内容？
   - ✅ 右上角是否显示你是协作者？

3. **告诉我结果**，我来测试API写入

---

## 快速参考数据

| 项目 | 值 |
|------|-----|
| 应用ID | `cli_a9208a79e2b99cc4` |
| 你的Open ID | `ou_aea2e859a6cee1f3a11e8c018eb88ec9` |
| 表格名称建议 | `满意解研究所_案例库V2` |
| 数据表名称建议 | `合伙人决策案例` |

---

## 注意事项

1. **权限已确认**：`base:app:create` ✅ 已授权
2. **如果报错**：检查 `app_token` 和 `table_id` 是否填对
3. **协作权限**：第四步是关键，和之前文档协作者逻辑一样

---

**文档版本**：V1.0  
**最后更新**：2026-03-09 14:30
