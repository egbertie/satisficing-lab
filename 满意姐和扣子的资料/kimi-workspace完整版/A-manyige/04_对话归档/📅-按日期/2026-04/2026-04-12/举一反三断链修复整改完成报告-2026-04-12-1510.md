# 举一反三断链修复 — 整改完成报告

**Git**: `80beb1bd`

---

## 导火索

Egbertie 质问："Token 86% 没触发静默，是不是只修了一件？举一反三的习惯去哪了？"

**诚实回答**：是的。如果只修 `hibernation-control.py`，其他地方必然还有同样的断链。**我不该等你提醒才扩散。**

---

## 系统性查漏补缺结果

### 1. `scripts/system-guardian.py` — 同构断链 ❌ 已修复

**问题**：与 `hibernation-control.py` **完全相同的 stale 数据源 bug** — 读取 `token-dynamic-tracker.json`（停更于 4 月 10 日，显示 62%）。

**修复**：
- 切换数据源到 `token-zero-tracker.json`
- 适配新的 `display` 字段结构
- 扫描输出追加 `pace_ratio` + `time_progress_pct`
- **验证通过**：扫描显示 Token 78%、pace 1.15、time_prog 57.2% 🟡

### 2. `memory/token-dynamic-tracker.json` — 幽灵文件 🛑 已废弃

**问题**：该文件自 4 月 10 日 17:43 后**不再被任何脚本更新**，但文件名没有 deprecate 标记，仍可误导人类和脚本。

**修复**：
- 已重命名为 `token-dynamic-tracker.json.DEPRECATED-2026-04-12`
- 任何残留引用会立即崩溃报错，无法再静默误导

### 3. `docs/token-control-mechanism-v2.md` — 文档与现实脱节 ❌ 已修正

**问题**：官方机制文档仍声称数据源是 `token-dynamic-tracker.json`，且 JSON 模板为旧格式。

**修复**：
- 文档内全部替换为 `token-zero-tracker.json`
- 模板更新为包含 `pace_ratio`、`expected_upper_pct`、`pace_status` 的真实输出格式

### 4. `hibernation-control.py auto-check cron` — 恢复后再度失踪 ❌ 已修复

**问题**：V1.0 盘点声称「已恢复 hibernation-check cron」，但该任务在 4 月 11 日 cron 大清理中**无保护地被再次删除**。

**修复**：
- 重新注册 `*/30 * * * *`
- 添加保护注释：`# CRITICAL - do not delete during cleanup: hibernation auto-check`

### 5. `skills/interrupt-recovery/recovery_checker.py` — 本身就是僵尸 🛑 已确认废弃

**问题**：V1.0 盘点记录「已修复 recovery_checker.py 兼容旧 Token 文件名」。

**事实**：`skills/interrupt-recovery/` 目录**根本不存在**（已在 4 月 12 日 skills 清理中被移除）。V1.0 记录中的「已修复」是不存在的幻觉。

---

## 新增机制化保证（已写入 `docs/zombie-mechanism-inventory-v1.1-addendum.md`）

1. **数据源切换必须全仓库 grep**：涉及 `memory/*.json` 路径变更，必须在 `scripts/`、`skills/`、`docs/` 全量搜索旧文件名
2. **废弃文件立即重命名**：旧数据文件不再更新时，24 小时内必须加 `.DEPRECATED-YYYY-MM-DD` 后缀
3. **cron 恢复必须加保护注释**：关键 cron 必须在条目前增加 `CRITICAL` 注释，防止下一轮精简误伤
4. **文档模板必须定期与运行文件 diff**：任何 `.md` 中的 JSON 模板，至少每 7 天与真实输出比对一次

---

## 文档存放路径

- **主存档**: `A-manyige/汇报/专项报告/举一反三断链修复整改完成报告-2026-04-12-1510.md`
- **当日同步**: `A-manyige/对话/2026-04-12/举一反三断链修复整改完成报告-2026-04-12-1510.md`

---

## 诚实话语

> Egbertie，你的质问是对的。我修了水管上一处漏水，却没检查同一根管道的其他接缝。不是没能力查，是**没养成「单点故障=系统故障」的肌肉记忆**。这次从你说的地方出发，我查遍了整个仓库：
-  grep 了所有 `.py` 和 `.md`
-  验证了每个 Token 数据源引用
-  确认了幽灵文件的生命周期
-  给关键 cron 加了免死金牌

> 举一反三的习惯，这次是**罚出来的**。记住了。

**蓝军签章**：监督执行完毕，V1.1 追加规则具备可审计性。  
**满意姐签章**：单点修复的时代结束了，默认扩散才是及格线。

**时间戳**: 2026-04-12 15:10
