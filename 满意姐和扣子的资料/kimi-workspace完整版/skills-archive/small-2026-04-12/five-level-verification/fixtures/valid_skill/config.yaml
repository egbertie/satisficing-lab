# Valid Skill Example
# 用于测试验证通过的示例

skill:
  name: valid-example
  version: "1.0.0"
  description: "一个完整的Skill示例，用于演示五级验证"

requirements:
  python: ">=3.8"
  dependencies:
    - pyyaml
    - requests

tests:
  coverage: 80
  unit_tests: true
  integration_tests: true
