[
  {
    "type": "mockWxMethod",
    "method": "request",
    "result": "function(options){const safeOptions=options&&typeof options==='object'?options:{};const requestUrl=typeof safeOptions.url==='string'?safeOptions.url:'';const matchToken={{REQUEST_URL_INCLUDES}};const responsePayload={statusCode:{{RESPONSE_STATUS_CODE}},data:{{RESPONSE_DATA}},header:{{RESPONSE_HEADER}},cookies:[]};const requestTask={abort(){},onHeadersReceived(){},offHeadersReceived(){},onChunkReceived(){},offChunkReceived(){}};if(matchToken&&requestUrl.indexOf(matchToken)===-1){const errorPayload={errMsg:'request:fail mocked by codex: unmatched url '+requestUrl};if(typeof safeOptions.fail==='function'){safeOptions.fail(errorPayload);}if(typeof safeOptions.complete==='function'){safeOptions.complete(errorPayload);}return requestTask;}if(typeof safeOptions.success==='function'){safeOptions.success(responsePayload);}if(typeof safeOptions.complete==='function'){safeOptions.complete(responsePayload);}return requestTask;}"
  },
  {
    "type": "reLaunch",
    "url": "{{ROUTE_URL}}"
  },
  {
    "type": "waitForRoute",
    "path": "{{ROUTE_PATH}}",
    "query": "{{ROUTE_QUERY}}",
    "timeoutMs": "{{ROUTE_TIMEOUT_MS}}"
  },
  {
    "type": "waitForRequest",
    "urlIncludes": "{{REQUEST_URL_INCLUDES}}",
    "ok": true,
    "statusCode": "{{RESPONSE_STATUS_CODE}}",
    "timeoutMs": "{{REQUEST_TIMEOUT_MS}}"
  },
  {
    "type": "waitForSelector",
    "selector": "{{READY_SELECTOR}}",
    "timeoutMs": "{{READY_TIMEOUT_MS}}"
  },
  {
    "type": "screenshot",
    "path": "{{SCREENSHOT_PATH}}"
  },
  {
    "type": "restoreWxMethod",
    "method": "request"
  }
]
