# 2026-06-03 全日工作终报

> 生成时间: 2026-06-03 19:10

---

## 一、核心产出

### 1. 腾讯云迁移策略方案 ✅
- 最划算方案：COS + EdgeOne Pages（静态 ¥56/年）+ 飞书多维表格（免费）
- 域名过渡期三阶段方案
- 飞书邮箱集成方案（MX/SPF/DKIM）
- 新域名审核中，过渡期 GitHub Pages 继续服务

### 2. 客户通道上线 ✅
- **飞书群机器人**：Webhook 推送，客户留言/注册实时通知
- **飞书多维表格**：🏢 客户数据中心，13 字段，user_access_token 创建（你可见）
- **前端页面**：`client.html`（快速咨询/完整注册/飞书表单 iframe）
- **飞书通知 SDK**：`feishu-notify.js`
- 首页增加「🤝 直接联系我们 · 客户通道」入口

### 3. 后端服务体系 v2.0 ✅
- **12 张数据表**：客户 360°画像 · 行为事件 · 会话链路 · 产品交付 · 留言 · 转介绍 · 邮件日志 · API审计 · 决策案例 · 系统配置 · 验证码
- **事件采集引擎**：`sri-track.js` 自动上报所有页面访问/点击/产品使用/决策行为
- **前端 API SDK**：`sri-api.js` 封装所有后端调用
- **飞书邮箱集成**：API 方式 + SMTP 备选，5 套邮件模板（欢迎/回复/交付/转介绍）
- **转介绍系统**：推荐码生成 → 点击/注册/付费全链路追踪
- **安全中间件**：JWT 鉴权 · IP 限流 · API 审计日志
- **驾驶舱管理 API**：总览/漏斗/日活/产品/事件/客户 6 个数据面板

### 4. 飞书操作关键教训 ⚠️
- **tenant_access_token 创建文件 = 用户不可见**（owner 是应用）
- **user_access_token 创建文件 = 用户可见**（owner 是本人）
- 完整 OAuth 流程：重定向 URL 配置 → 浏览器授权 → code 换 token
- 已刻入 MEMORY.md 反模式章节

---

## 二、技术架构

```
前端层: GitHub Pages (HTML/CSS/JS) + sri-track.js + sri-api.js
中端层: Flask REST API (JWT/限流/日志) · 10+ endpoints
后端层: SQLite 12表 + 飞书集成(通知/表格/邮箱)
驾驶舱: /api/admin/* 6面板实时数据
```

### API 清单

| 路由 | 方法 | 功能 |
|------|------|------|
| `/api/health` | GET | 健康检查 |
| `/api/auth/register` | POST | 注册 |
| `/api/auth/login` | POST | 登录 |
| `/api/auth/me` | GET | 当前用户 |
| `/api/auth/logout` | POST | 登出 |
| `/api/contact` | POST | 留言 |
| `/api/inquiries` | GET | 留言记录 |
| `/api/profile` | GET/PATCH | 客户档案 |
| `/api/deliveries` | GET | 已购产品 |
| `/api/track` | POST | 事件上报 |
| `/api/session/start` | POST | 开始会话 |
| `/api/session/end` | POST | 结束会话 |
| `/api/referral/create` | POST | 生成推荐码 |
| `/api/referrals` | GET | 推荐记录 |
| `/api/admin/dashboard/summary` | GET | 驾驶舱总览 |
| `/api/admin/dashboard/funnel` | GET | 获客漏斗 |
| `/api/admin/dashboard/dau` | GET | 日活数据 |
| `/api/admin/dashboard/products` | GET | 产品使用 |
| `/api/admin/dashboard/events/recent` | GET | 事件流 |
| `/api/admin/dashboard/customers` | GET | 客户列表 |

---

## 三、关键决策

| 决策 | 内容 |
|------|------|
| 技术栈 | Flask + SQLite + JWT + 飞书全家桶 |
| 数据架构 | 12 表颗粒度细化到每次点击/每个决策环节 |
| 邮件方案 | 飞书邮箱 API 优先，SMTP 备选 |
| 安全策略 | JWT 72h 过期 + IP 限流 + 审计日志 |
| PLG 策略 | 事件漏斗驱动获客/激活/付费/转介绍全链路 |

---

## 四、待办事项

### P0 - 本周
- [ ] 飞书邮箱 API 配置 user_token 权限（mail:user_mailbox.message:send）
- [ ] 启动本地服务器完整体验 v2.0 全流程
- [ ] 驾驶舱 admin-windows.html 对接管理 API
- [ ] 所有 HTML 页面注入 sri-track.js（自动事件采集）

### P1 - 等腾讯云
- [ ] 飞书域名邮箱配置（MX/SPF/DKIM）
- [ ] 腾讯云 CVM 部署后端
- [ ] 网站整体迁移到腾讯云 COS + EdgeOne

### P2 - 后续
- [ ] 微信小程序后端对接
- [ ] SSL 证书配置（腾讯云免费）
- [ ] 自动备份 + 异地容灾

---

## 五、反思

1. **犯了之前明确记录过的错误**：用 tenant_access_token 创建文件 → MEMORY.md 已有这条教训，但仍踩坑。根因是上次没有链接到飞书 API 创建文件的具体场景。已补充完整 OAuth 流程记录。

2. **git rebase + stash 互动导致文件丢失**：client.html 重建了两次。以后清理 git 状态时先确认 stash 内容包含新文件。

3. **飞书 OAuth 流程需要用户手动授权**：这是正确的设计（安全），但需要提前告知用户需要打开浏览器。把这个流程文档化了。

---

> 📁 本报告将同步到 `对话/2026-06-03/` 和飞书「扣红协作-邮政通道」
